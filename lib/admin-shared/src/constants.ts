/** Portugal ecommerce defaults */
export const DEFAULT_CURRENCY = "EUR" as const;
export const DEFAULT_LOCALE = "pt-PT" as const;
export const SUPPORTED_LOCALES = ["pt", "en"] as const;

export const ADMIN_API_VERSION = "v1" as const;
export const ADMIN_API_PREFIX = `/api/${ADMIN_API_VERSION}/admin` as const;
