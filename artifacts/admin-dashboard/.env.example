# Next.js admin dashboard
NEXT_PUBLIC_APP_NAME=Samphone Admin
NEXT_PUBLIC_DEFAULT_LOCALE=pt

# Backend proxy target (see next.config.ts rewrites)
ADMIN_API_URL=http://127.0.0.1:8090

# Public API base (browser calls /api → rewrite)
NEXT_PUBLIC_ADMIN_API_URL=/api/v1/admin
